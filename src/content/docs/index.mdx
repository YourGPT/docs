---
title: "YourGPT Documentation"
description: Get started with YourGPT Chatbot - the AI-first no-code platform for building custom AI Agents and chatbots.
template: splash
hero:
  title: YourGPT Documentation
  tagline: Welcome to documentation page for YourGPT.
---
import { InfoCard, CARDDATA } from '../../components/Landing/InfoCard.jsx';
import { MdOutlineLaptopMac } from 'react-icons/md';
import { HiRocketLaunch } from 'react-icons/hi2';
import { TbArrowsCross } from 'react-icons/tb';
import { GoArrowRight } from 'react-icons/go';
import { GrServices } from 'react-icons/gr';
import { Note } from '~/components';
import { Image } from 'astro:assets';
import darkLogo from '~/assets/ygc-logo/yourgpt-logo-dark-symbol.svg';
import whiteLogo from '~/assets/ygc-logo/yourgpt-logo-white-symbol.svg';
import Discord from '../../components/Landing/Discord.astro';

<div className="flex flex-col lg:flex-row lg:justify-between lg:items-center gap-12 lg:gap-36 pb-16 lg:px-12 dark:bg-[url('/assets/round-pattern.svg')] bg-cover bg-center bg-no-repeat px-8 !py-16 !my-9 rounded-xl bg-gray-100 dark:bg-black/[0.08]">
  <div className='order-2 lg:order-1'>
    <div className='tracking-tight text-4xl md:text-6xl font-bold break-words max-w-[600px] !leading-[50px] md:!leading-[70px] lg:!leading-[75px] text-[#374151] dark:text-white/95'>YourGPT Documentation</div>
    <p className='text-base sm:text-lg max-w-[660px] text-gray-600 dark:text-white tracking-tight'>
      Discover comprehensive guides, tutorials, and reference materials to help you make the most of YourGPT's powerful features and capabilities.
    </p>
  </div>
  <div className='w-full order-1 lg:order-2 max-w-[280px] !mt-0'>
    <img id="hero-symbol-logo" className="!mt-0" style={{width:"100%"}} alt="YourGPT Symbol" src={darkLogo.src} />
  </div>
</div>

<div className="grid grid-cols-1 gap-3  md:grid-cols-2 lg:grid-cols-3 py-6 !mt-0 place-items-center">
{
  CARDDATA.map((card, index) => (
    <InfoCard
      key={index}
      title={card.title}
      icon={card.icon}
      description={card.description}
      link={card.link}
      linkText={card.linkText}
      iconColor={card.iconColor}
      buttonColor={card.buttonColor}
      bgPattern={card.bgPattern}
      target={card.target}
    />
  ))
}
</div>



<Discord title="Have a question or want to get involved?" cta="Join our Discord"/>

<script>
{`
  document.addEventListener('DOMContentLoaded', () => {
    const heroLogo = document.getElementById('hero-symbol-logo');
    if (!heroLogo) return;

    const darkLogoPath = '${darkLogo.src}';
    const whiteLogoPath = '${whiteLogo.src}';

    const applyHeroLogo = () => {
      const isDarkMode = 
        document.documentElement.classList.contains('dark') ||
        document.documentElement.getAttribute('data-theme') === 'dark' ||
        document.documentElement.dataset.theme === 'dark';
      
      heroLogo.src = isDarkMode ? whiteLogoPath : darkLogoPath;
    };

    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        if (mutation.type === 'attributes' && 
            (mutation.attributeName === 'class' || 
             mutation.attributeName === 'data-theme')) {
          applyHeroLogo();
        }
      });
    });

    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['class', 'data-theme'],
    });

    applyHeroLogo();
  });
`}
</script>

